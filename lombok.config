config.stopBubbling = true

# Marks Lombok-generated members with @lombok.Generated so JaCoCo excludes them.
# Without this, generated getters/setters/equals/hashCode/toString dominate the
# coverage report and hide how much hand-written logic is actually tested.
lombok.addLombokGeneratedAnnotation = true
